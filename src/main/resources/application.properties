com.verdino.balancing.model.db.type=filesystem.local
com.verdino.balancing.model.db.source=src\\test\\resources\\data-01
com.verdino.balancing.bankfile.reader.uploadDir=C:\\Users\\primo\\git\\Accounting\\src\\test\\resources\\tmpFileStorage
spring.mvc.view.prefix: /WEB-INF/jsp/
spring.mvc.view.suffix: .jsp

spring.datasource.url=jdbc:mysql://localhost:3306/balance_1.6.0-test.db?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=secret!1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#spring.mvc.static-path-pattern=/static/**

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=./uploads

# logging level
logging.level.org.springframework=ERROR
logging.level.controllers=ERROR

# output to a file
logging.file=app.log

# temp folder example
#logging.file=${java.io.tmpdir}/app.log

logging.pattern.file=%d %p %c{1.} [%t] %m%n

logging.pattern.console=%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n